# RAVX — ملاحظات هذا التحديث (إصلاح نظام الاشتراكات بالكامل)

## المشاكل اللي كانت موجودة ولماذا حصلت

### 1) كود التجربة يُستخدم أكثر من مرة من الموقع
كان في **نظامان منفصلان** يكتبان على نفس الملف `permissions.json`:

- البوت (`index.js`) يحمّل الملف في الذاكرة عند التشغيل، وكل تعديل يحفظ **النسخة
  اللي بذاكرته** كاملة.
- الموقع (`src/server/server.js`) يقرأ ويكتب نفس الملف بشكل مستقل.

فلما يشفّر العضو من الموقع، الموقع يزيد `usedEncrypts` إلى 1… وبعدها أول كتابة من
البوت (أي تفعيل كود لأي عضو، أو دورة الفحص) تحفظ نسخته القديمة من الذاكرة فيرجع
`usedEncrypts` إلى 0 — أي **رصيد التجربة يرجع من جديد**. هذه هي الثغرة الأساسية.

### 2) الموقع يتأخر في سحب الصلاحية
صلاحية التشفير في الموقع كانت مأخوذة من **الجلسة** (`user.canEncrypt`) اللي
تتحدّث كل 3 دقائق فقط. فحتى لو انسحبت الرتبة، يظل المستخدم قادراً على الضغط
والتشفير خلال هذه الفجوة. وسحب الرتبة نفسه كان ينتظر دورة فحص كل 60 ثانية.

### 3) «لديك اشتراك فعال بالفعل» رغم انتهاء التجربة
عند استهلاك التجربة **من الموقع**، كان الموقع يضع `exhausted: true` لكن **بدون**
تعديل `expiresAt` (يبقى بعد 3 أيام في المستقبل). ودالة التفعيل كانت تتحقق من
`expiresAt` فقط، فتعتبره «اشتراك فعّال» وترفض أي كود تجربة جديد لمدة 3 أيام،
مع إن رتبته انسحبت ورصيده خلص. لذلك العضو يحصل على الرسالة ولا يقدر يستخدم كود ثانٍ.

---

## الحل: محرك اشتراكات واحد مشترك

ملف جديد: **`src/shared/subscriptions.js`** — صار المصدر الوحيد للحقيقة، يستعمله
البوت والموقع معاً:

- **لا يوجد أي نسخة في الذاكرة إطلاقاً.** كل قراءة من القرص لحظياً، وكل كتابة
  ذرّية (`tmp` + `rename`) فما يتلف الملف لو انقطعت العملية.
- **قفل مشترك بين العمليات** (`.ravx-perm.lock`) — جُرِّب فعلياً بتشغيل 6 عمليات
  منفصلة تحاول استهلاك نفس عملية التجربة في نفس اللحظة: واحدة فقط نجحت، والخمس
  الباقية رُفضت، والنتيجة النهائية `usedEncrypts = 1`.
- **حجز ثم تأكيد (reserve → commit):** يُحجز الرصيد **قبل** بدء المعالجة، ويُؤكد بعد
  نجاح التشفير. لو فشل التشفير يُرجَّع الحجز تلقائياً فلا يخسر العميل عمليته.
  وهذا يمنع: ضغطتين سريعتين، أو طلب من الموقع وطلب من الديسكورد بنفس الثانية.
- **قاعدة واحدة لتعريف «اشتراك فعّال»:** غير مستهلك + غير منتهٍ + فيه رصيد متبقٍ.
  أي حالة أخرى = منتهٍ، ويُسمح فوراً بتفعيل كود جديد.

## ما الذي تغيّر عملياً

| المشكلة | الوضع الآن |
|---|---|
| تكرار التجربة من الموقع | مستحيل — الرصيد محجوز بقفل مشترك، والتحقق من نفس السجل في الطرفين |
| تأخر سحب الصلاحية بالموقع | السحب فوري لحظة انتهاء الرصيد: تُقفَل الصلاحية + تُسحب الرتبة من Discord + تُبطَل كل جلسات العضو في الموقع في نفس الطلب |
| «لديك اشتراك فعال بالفعل» | تظهر فقط لو الاشتراك فعّال حقيقةً، ومعها الباقة والرصيد المتبقي ووقت الانتهاء. بعد استهلاك التجربة يقدر يفعّل كوداً جديداً **فوراً** |
| التجربة من الموقع أو من الديسكورد | الاثنان يعملان بنفس الرصيد تماماً، ونفس قفل الآي بي في السكربت الناتج. عملية واحدة فقط، من أي مكان يبدأها |
| اعتماد الموقع على الرتبة | صار السجل هو المرجع؛ يفعّل كوداً في Discord → يقدر يشفّر من الموقع فوراً بدون انتظار مزامنة، وينتهي رصيده → يُرفض فوراً حتى لو بقيت الرتبة عالقة عنده |
| دورة الفحص | كانت 60 ثانية، صارت 15 ثانية، وهي الآن **شبكة أمان فقط** لأن السحب الأساسي فوري؛ ولو فشل نداء Discord يُعاد تلقائياً في الدورة التالية |

## أشياء إضافية أُصلحت

- **باقة التجربة**: `days: 0` في ملف الأكواد (كانت تنتهي لحظة تفعيلها) → صارت 3 أيام
  نافذة لاستخدام العملية الواحدة، والمصدر الوحيد للمدد هو `PLAN_LIMITS`.
- **السجلات لا تُحذف** عند الانتهاء، بل تُعلَّم كمنتهية وتُؤرشف القديمة في
  `subscription_history.json`. حذف السجل كان يفتح ثغرة: لو فشل سحب الرتبة يختفي
  ما يمنع التشفير.
- **توليد الأكواد**: صار بـ `crypto` بدل `Math.random` (أكواد تُباع، لازم تكون غير
  قابلة للتخمين)، وبطول 12 حرفاً، وبدون الحروف الملتبسة `O/0/I/1`.
- **صفحة لوحة التحكم** تعرض الآن: الباقة، الرصيد المتبقي، ووقت الانتهاء، وتتحدث
  تلقائياً كل 30 ثانية وبعد كل عملية تشفير.
- حُذف `db.js` المكرر في الجذر (كان يشير لمسارات `../../storage` خارج المشروع).

## الأكواد

تم **إلغاء كل الأكواد القديمة** وتوليد **500 كود جديد** (100 لكل باقة) في
`permission_codes.json`، ومصدّرة في `ravx-store-codes.txt`.

⚠️ الأكواد القديمة لم تعد تعمل. وزّع الملف الجديد فقط.

لتوليد دفعة جديدة لاحقاً:

```
node scripts/generate-codes.js            # يكمل كل باقة إلى 100 كود متاح (لا يلمس القديم)
node scripts/generate-codes.js 200        # يكملها إلى 200
node scripts/generate-codes.js 100 --fresh  # يلغي كل القديم ويولّد 100 جديد لكل باقة
```

## التشغيل

```
npm install
npm start
```

المتغيرات المطلوبة في `.env` (راجع `.env.example`): توكن البوت، روم الأكواد،
رتبة الاشتراك، وإعدادات OAuth للموقع. لسحب الرتب من **الموقع** مباشرة لازم يكون
`DISCORD_BOT_TOKEN` و`DISCORD_GUILD_ID` موجودين، وإلا يتكفّل البوت بالسحب خلال
15 ثانية (والتشفير يظل مرفوضاً في الحالتين).

## بخصوص «بدون أي ثغرات»

ما فيه ضمان 100% لأي نظام. الحمايات المطبّقة والمختبَرة هنا:
كل قرار من الخادم لا من الواجهة، قفل مشترك بين كل العمليات، حجز قبل الاستهلاك،
سجل لا يُحذف، Rate Limiting على نقاط الدخول الحساسة، تحقق من صيغة الآي بي قبل
إدخاله في ملفات Lua، وسجلات مراقبة لكل تفعيل اشتراك.
