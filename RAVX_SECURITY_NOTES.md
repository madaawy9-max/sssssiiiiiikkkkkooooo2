# RAVX — ملاحظات هذا التحديث

## 1) نظام الاشتراكات والرتب (تم بناؤه/إصلاحه بالكامل)

كان هناك خلل جوهري: كود "تجربة" كان `days: 0`، فينتج `expiresAt = Date.now() + 0`
أي ينتهي **فوراً** لحظة تفعيله — قبل ما يقدر العضو يستخدم عملية التشفير الوحدة
أصلاً. تم إصلاحه بإعطاء التجربة نافذة حقيقية (3 أيام) لاستخدام عمليتها الواحدة.

- 🧪 **تجربة**: عملية تشفير واحدة فقط. بعد استخدامها (أو بعد 3 أيام) يُقفل
  الكود نهائياً وتُسحب الرتبة، ولا يمكن تكرار التشفير — تم اختباره فعلياً
  بمحاكاة استهلاك الرصيد والتأكد أن المحاولة الثانية تُرفض، **حتى لو فشل حذف
  الرتبة من Discord لأي سبب** (كان هذا ثغرة حقيقية اكتشفتها أثناء الاختبار
  وأصلحتها — راجع القسم 3).
- ⏱️ **يومي**: 24 ساعة ثم سحب تلقائي للرتبة.
- 🥉 **أسبوعي**: 7 أيام ثم سحب تلقائي.
- 🥈 **شهري**: 30 يوم ثم سحب تلقائي.
- 👑 **مدى الحياة**: بدون انتهاء إطلاقاً.

**الحماية:**
- منع استخدام نفس الكود مرتين (يُعلَّم `used` داخل قفل تسلسلي يمنع أي سباق).
- منع أكثر من اشتراك فعّال لنفس الحساب (تم اختباره).
- التخزين في ملفات JSON دائمة على القرص (`permissions.json`,
  `permission_codes.json`) وليس بالذاكرة — يبقى الاشتراك صحيحاً ويكمل حساب
  مدته بعد أي إعادة تشغيل للبوت لأن وقت الانتهاء طابع زمني مطلق.
- سجلات إدارية (`subscription_logs.json` + قناة Discord اختيارية
  `SUBSCRIPTION_LOG_CHANNEL_ID`): من فعّل، متى، أي باقة، أي كود.
- تم تصدير 500 كود جاهز (100 لكل باقة) عبر `scripts/generate-codes.js`،
  موجودة في `permission_codes.json` وملخّصة في `new-codes-export.txt`.

## 2) إصلاحات الموقع

**السبب الأرجح لأعراض "تسجيل الخروج عند تغيير الصفحة" و"تعليق الصفحة ثم رجوعها":**
الجلسات كانت مخزّنة في الذاكرة فقط (`Map`)؛ أي إعادة تشغيل للسيرفر (شائعة على
استضافات مجانية مثل Render عند التوقف المؤقت لعدم النشاط) كانت تمسح كل
الجلسات فجأة، بينما التحقق من صلاحيات Discord كان يُعاد بنداءين لـ API في
**كل** تحميل صفحة، فيسبب تعليقاً عند أي بطء مؤقت من Discord.

تم:
- حفظ الجلسات على القرص (`storage/sessions.json`) — تبقى صالحة عبر
  إعادة التشغيل حتى تنتهي مدتها الفعلية (7 أيام).
- تخفيف التحقق من Discord إلى مرة كل 3 دقائق لكل جلسة بدل كل تنقل.
- Rate Limiting بسيط على: تسجيل الدخول، رد OAuth، التشفير، التحميل، البحث عن كود.
- التحقق من صيغة IP/دومين السيرفر قبل إدخاله لمحرك التشفير.
- تطبيق سقف عدد عمليات التشفير (حسب الباقة) على تشفير **الموقع** أيضاً، وليس
  فقط تشفير الديسكورد، عبر قراءة نفس `permissions.json`.
- منع تنفيذ طلبي تشفير متزامنين لنفس المستخدم.
- رؤوس أمان أساسية (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`).

## 3) ثغرة اكتُشفت وأُصلحت أثناء الاختبار (مهم)

أول نسخة كتبتها كانت **تحذف** سجل صلاحية العضو بمجرد استهلاك رصيد التجربة،
وتحاول حذف رتبة Discord كخطوة أخيرة. المشكلة: لو فشل حذف الرتبة من Discord
(توكن غير مضبوط، تأخير مؤقت من الـAPI...)، يختفي السجل المحلي الذي يمنع
التشفير، فيصير العضو قادراً على تكرار التشفير مجاناً رغم أن رتبته ما زالت
عالقة عنده! اختبرتها فعلياً وأكدت الثغرة، ثم أصلحتها بحيث يبقى السجل محفوظاً
دائماً بحالة "مستهلك" (`exhausted: true`) بدل حذفه، فيستمر الحظر بغض النظر
عن نجاح حذف الرتبة من عدمه. أي إعادة استخدام لاحقة (شراء كود جديد) تستبدل
هذا السجل بالكامل بسجل جديد نظيف.

## 4) بخصوص "بدون أي ثغرات"

لا يوجد ضمان بخلو أي موقع من الثغرات 100%. الحمايات المطبّقة فعلياً:
- التحقق من الصلاحيات دائماً من الخادم (لا اعتماد على الواجهة وحدها).
- إعادة التحقق من صلاحية Discord دورياً، مع تحديث فوري عند تسجيل الدخول.
- حماية الـAPI بـ Rate Limiting والتحقق من المدخلات.
- سجلات مراقبة لعمليات الاشتراك.

قبل التشغيل:
```
npm install
npm start
```
