# إضافات هذا التحديث: ملف اللوق + فك الحماية للتعديل

## 1) ملف اللوق (Log)

ملف جديد **`src/shared/logger.js`** — مصدر واحد يستعمله الموقع والبوت معاً.

- يسجّل كل حدث سطراً واحداً بصيغة JSON في `storage/logs/app-YYYY-MM-DD.log`
  (ملف جديد كل يوم حتى لا يكبر ملف واحد بلا حدود)، مع طباعته بالكونسول أيضاً
  (يظهر بلوق الاستضافة مثل Render/PM2).
- الأحداث المسجَّلة حالياً: تسجيل الدخول (`auth.login`)، نجاح/فشل التشفير من
  الموقع (`encrypt.success` / `encrypt.failed`)، نجاح/فشل فك الحماية
  (`unprotect.success` / `unprotect.failed`)، فك الحماية من سطر الأوامر
  (`unprotect.cli`)، وأي خطأ غير متوقع بالسيرفر (`server.unhandled`).
- **لوحة الأدمن** بالموقع (`/dashboard.html`) تعرض الآن قسم "سجل الأحداث"
  (يظهر للأدمن فقط) يقرأ آخر 200 حدث عبر `GET /api/logs` (أدمن فقط).
- `storage/logs/` مثل باقي `storage/` — يبقى على القرص بين عمليات إعادة
  التشغيل طالما الاستضافة توفر قرصاً دائماً لمسار `storage/` (نفس ملاحظة
  الأكواد والجلسات في `RAVX_SECURITY_NOTES.md`).

## 2) فك الحماية للتعديل (Unprotect)

المشكلة: التمويه القديم (XOR) لا رجوع منه — لو احتجت تعدّل سكربت سبق تشفيره
(حتى لو كان ملفك أنت)، ما فيه طريقة لإرجاعه لنص مقروء.

الحل: **`unprotectLuaBlob` / `unprotectFiles`** في
**`src/shared/protection-engine.js`** — نفس محرك التشفير بالضبط لكن بالاتجاه
المعاكس:

- مفاتيح التمويه (`xk1`, `xk2`, `xmul`) عشوائية لكل ملف، لكنها مكتوبة كأرقام
  صريحة داخل دالة فك الشيفرة نفسها بملف Lua الناتج (لازم يقدر يفك نفسه وقت
  التشغيل)، فيكفي قراءتها من الملف وتطبيق نفس خطوات فك الشيفرة بترتيبها
  بالضبط لاسترجاع الكود الأصلي حرفياً.
- بعد فك التمويه، تُزال كتلة "RAVX-TEAM SERVER SECURITY & IP CHECK" المدمجة
  (إن وُجدت في ملفات server/main) تلقائياً — فيرجع الملف لكود المطوّر الأصلي
  فقط، جاهز للتعديل وإعادة التشفير من جديد بدون أي تكرار.
- يعمل على **أي أرشيف مشفَّر بنفس القالب**، حتى لو كان قديماً جداً ومُشفَّراً
  بنسخة سابقة من الأداة قبل هذا التحديث — لأن كل المعلومات اللازمة للفك
  موجودة داخل الملف الناتج نفسه، وليست مخزَّنة بقاعدة بيانات منفصلة.

### طرق الاستخدام

**من الموقع (لوحة الأدمن):** قسم "فك الحماية للتعديل" في `/dashboard.html`،
يظهر للأدمن فقط عمداً — حصر الصلاحية بالأدمن يمنع أي مستخدم عادي من فك حماية
سكربت غيره اشتراه بنفس الأداة. ترفع ZIP المشفَّر → تحصل على ZIP جديد بنفس
البنية لكن بملفات Lua مقروءة، مع كود تحميل مثل باقي السكربتات.

**من سطر الأوامر (بدون موقع أو بوت إطلاقاً):**
```
node scripts/unprotect.js path/to/protected.zip
node scripts/unprotect.js path/to/protected.zip path/to/output.zip
```
مفيد لملفات قديمة جداً محفوظة عندك محلياً وما مرّت بالموقع أصلاً.

بعد التعديل، أعد رفع الملف عبر لوحة التشفير العادية (الموقع أو الديسكورد) —
يُشفَّر من جديد بنفس الآلية (`processAndProtectFiles`) بحارس آي بي جديد كالمعتاد.

⚠️ فك الحماية لا يحتاج كود اشتراك ولا يستهلك رصيداً — هو أداة إدارة داخلية
للأدمن على أرشيفات موجودة أصلاً عندك، وليست عملية تشفير جديدة.

## 3) تغيير الآي بي بعد البيع — "حيّ" بدون إرسال ملف جديد

المشكلة اللي طرحتها: بعت سكربت مشفَّر لعميل، وبعدين قال "أبغى أغيّر الآي بي
(غيّر سيرفره/استضافته)". قبل هذا التحديث ما فيه طريقة غير إرجاع فك الحماية،
تعديل، وإعادة تشفير بملف جديد وإرساله له من جديد.

**الحل:** الملف المشفَّر الآن **لا يحمل أي آي بي بداخله إطلاقاً**. بدل ذلك
يحمل فقط "كود الترخيص" (نفس كود التحميل بالموقع، مثل `RAVX-WD3V3JY8QL`)،
وعند تشغيل السيرفر يسأل خادمك مباشرة: *"هل الآي بي اللي أنا شغّال عليه الآن
مسموح لهذا الكود؟"* عبر `GET {BASE_URL}/api/license/الكود` — وخادمك يعرف آي
بي الطالب من الطلب نفسه مباشرة (بدون أي اعتماد على `api.ipify.org` كما بالنسخة
القديمة) ويقارنه بالآي بي المخزَّن حالياً لهذا الكود.

**يعني عملياً:** لما تبغى تغيّر آي بي عميل، تروح للوحة الأدمن (`/dashboard.html`)
→ قسم **"تغيير الآي بي المرخَّص (حيّ)"** → تدخل كود السكربت والآي بي الجديد
→ تضغط تحديث. **ما ترسل له أي ملف جديد** — في أول مرة (أو ثاني مرة لو كان
السكربت شغّال بالفعل) يعيد فيها تشغيل موردته، يتحدّث الترخيص تلقائياً.

### تفاصيل تقنية
- نقطة النهاية العامة `GET /api/license/:code` بدون تسجيل دخول (لأن الطلب
  يجي من خادم FiveM للعميل مباشرة، لا من متصفح)، بمعدل محدود (Rate Limit)
  لمنع الإغراق، وتسجّل كل محاولة مرفوضة باللوق (`license.denied`).
- نقطة الإدارة `POST /api/script/:code/ip` — أدمن فقط، تتحقق من صيغة
  الآي بي/الدومين قبل الحفظ، وتسجّل كل تغيير باللوق (`license.ip_changed`).
- `db.createPendingScript` / `db.finalizeScript` (في `src/database/db.js`):
  الآن نحجز كود التحميل **قبل** التشفير (بدل بعده) عشان ندمجه داخل ملف Lua
  كمعرّف — سواء التشفير جاء من الموقع أو من الديسكورد. لو فشلت المعالجة
  يُحذف السجل المبدئي تلقائياً فلا يبقى كود "معلَّق" بلا ملف.
- **مطلوب:** متغير البيئة `BASE_URL` لازم يكون مضبوطاً على دومين موقعك
  الفعلي (نفس المذكور أصلاً بـ`SETUP.md`) — بدونه، التشفير يرفض العملية
  بخطأ واضح بدل ما ينتج ملفاً بفحص ترخيص معطوب.
- ملفات مشفَّرة **قبل** هذا التحديث (بالطريقة القديمة، آي بي ثابت بالملف)
  تستمر تعمل بمنطقها القديم كما هي — لا تحتاج أي شيء إضافي، وتقدر تفكّها
  وتعيد تشفيرها بالطريقة الجديدة لو حبيت تفعّل لها ميزة التغيير الحيّ.

### تعدد العناوين لنفس الكود + كشف IPv4 الحقيقي دائماً

بعض السيرفرات تتصل بالإنترنت أحياناً عبر IPv4 وأحياناً عبر IPv6 (حسب شبكة
مزوّد الاستضافة)، وهذا كان يسبب رفض عميل شرعي أو ظهور عنوان IPv6 غريب بدل
الـIPv4 المعروف. الحل المطبَّق:

1. **الملف نفسه يحدّد عنوانه على IPv4 تحديداً دائماً** قبل ما يسأل خادم
   الترخيص — عبر `https://api4.ipify.org` (له سجل DNS من نوع A فقط بلا
   AAAA، فيُجبر أي اتصال به على IPv4 مهما كانت تفضيلات الشبكة)، ثم يرسل هذا
   العنوان كباراميتر (`?ip=...`) مع طلب الترخيص. خادمك يعتمد هذا العنوان
   المُبلَّغ به (لو كان صالحاً) بدل عنوان الاتصال الخام، فيطابق دائماً نفس
   الـIPv4 اللي تعرفه وتبيعه للعميل — بالضبط زي المثال اللي عطيتني إياه
   (`45.137.98.231`).
2. **حقل الآي بي يقبل أكثر من عنوان مفصول بفاصلة** (`src/server/server.js`:
   `isValidTarget` / `matchesTargetIp`) — لو حبيت تسجّل أكثر من عنوان لنفس
   العميل (مثلاً سيرفر احتياطي)، أو لو عميل معيّن ما زال يوصل أحياناً بعنوان
   IPv6 لأي سبب، تقدر تضيفه بجانب الـIPv4 من نفس الفورم بلوحة الأدمن.
3. **لوحة الأدمن → "سجل الأحداث"**: أي محاولة مرفوضة الآن فيها زر
   "➕ ثبّت هذا العنوان لهذا الكود" يعبّي لك فورم تغيير الآي بي بالكود
   والعنوان المرفوض تلقائياً، لو تأكدت إنه عميلك الشرعي واحتجت تضيفه.
