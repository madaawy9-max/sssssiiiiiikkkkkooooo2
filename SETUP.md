# إعداد النسخة المعدلة

## المتطلبات

تحتاج النسخة إلى Node.js 18 أو أحدث، وأوامر `zip` و`unzip` متاحة على النظام. ثبّت الاعتمادات عبر:

```bash
npm install
npm start
```

## متغيرات البيئة الأساسية

```env
DISCORD_BOT_TOKEN=توكن_البوت
DISCORD_CLIENT_ID=معرف_تطبيق_ديسكورد
DISCORD_CLIENT_SECRET=سر_تطبيق_ديسكورد
DISCORD_REDIRECT_URI=https://your-domain.example/api/auth/callback
DISCORD_GUILD_ID=معرف_السيرفر
ENCRYPT_ROLE_ID=معرف_رتبة_التشفير
ADMIN_USER_IDS=معرفات_الأدمن_مفصولة_بفواصل
SESSION_SECRET=قيمة_عشوائية_طويلة
MAX_UPLOAD_BYTES=5368709120
PORT=3000
BASE_URL=https://your-domain.example
```

صلاحية التشفير تُحسب من Discord عند تسجيل الدخول: Administrator أو رتبة `ENCRYPT_ROLE_ID`. لوحة الإدارة لا تظهر إلا لمن لديه Administrator أو معرف موجود في `ADMIN_USER_IDS`. لا توجد صلاحيات تشفير موثوقة من الواجهة وحدها؛ الخادم يعيد التحقق من الجلسة.

⚠️ `BASE_URL` أصبح **مطلوباً فعلياً** الآن (وليس فقط لروابط التحميل): الملفات المشفَّرة تحمل كود ترخيص فقط وتسأل `{BASE_URL}/api/license/الكود` وقت التشغيل بدل ما تحمل آي بي ثابت بداخلها — هذا يسمح بتغيير الآي بي المرخَّص لأي عميل لاحقاً من لوحة الأدمن بدون إرسال ملف جديد له. راجع `RAVX_UNPROTECT_AND_LOGS_NOTES.md` للتفاصيل.

## ملاحظات الملفات الكبيرة

الرفع يُكتب مباشرة إلى ملف مؤقت عبر streaming، لذلك لا يتم تحميل ZIP كاملًا إلى الذاكرة. الحد الافتراضي 5GB. يحتاج فك الضغط والتشفير مساحة قرص مؤقتة إضافية، ويُنصح بتوفير مساحة لا تقل عن ضعف حجم ZIP مع مساحة للناتج.

محرك الموقع يفك ZIP، يعالج جميع ملفات Lua بحسب النمط، ثم يعيد ZIP ويحفظه في `storage/uploads`. الأنماط هي `target` و`full` و`none`؛ ملفات server/main تحتفظ بحارس الترخيص حتى في النمط `none`.

## أمان مهم

النسخة المعدلة أزالت الأسرار الافتراضية المكشوفة من الكود. يجب تدوير Bot Token وWebhook السابقين في Discord لأنهما ظهرا داخل النسخة الأصلية، وعدم وضع أي أسرار داخل Git أو الأرشيف.


## الاشتراكات
PERMISSION_CODES_CHANNEL_ID=روم_استلام_الأكواد
GRANT_PERMISSION_ROLE_ID=رتبة_الاشتراك_الواحدة
SUBSCRIPTION_LOG_CHANNEL_ID=روم_سجلات_الإدارة (اختياري — يستلم تنبيه بكل عملية تفعيل)

### كيف يشتغل نظام الأكواد الآن
- كل باقة لها "نوع" (`type`) في `permission_codes.json` يحدد مدتها الحقيقية وسقف عدد عمليات التشفير، بدل الاعتماد على حقل `days` وحده:
  - `single_script` (تجربة): نافذة 3 أيام لاستخدام عملية تشفير واحدة فقط. بعد أول عملية تشفير تُسحب الرتبة تلقائياً ويُقفل الوصول نهائياً، ولا يمكن تكرار التشفير حتى لو حاول استخدام حسابه من الموقع أو من الديسكورد.
  - `unlimited_24h` (يومي)، `unlimited_7d` (أسبوعي)، `unlimited_support` (شهري): تشفير غير محدود العدد خلال المدة، وتُسحب الرتبة تلقائياً عند الانتهاء (فحص كل دقيقة + عند إقلاع البوت).
  - `lifetime` (مدى الحياة): بدون انتهاء إطلاقاً.
- **منع إعادة استخدام نفس الكود**: كل كود يُعلَّم `used` فور أول استخدام ناجح، داخل قفل تسلسلي (mutex) يمنع أي سباق لو انضغط الزر مرتين أو انبعثت رسالتان بنفس اللحظة.
- **منع أكثر من اشتراك فعّال لنفس الحساب**: قبل قبول أي كود جديد، يتحقق النظام أن العضو ما عنده اشتراك نشط حالياً (سواء وقتي لم ينتهِ، أو مدى الحياة).
- **قاعدة بيانات حقيقية على القرص وليست بالذاكرة فقط**: `permissions.json` و`permission_codes.json` و`subscription_logs.json` كلها ملفات JSON دائمة على القرص. أي إعادة تشغيل للبوت تُحمَّل من جديد وتكمل حساب المدة MDN بشكل صحيح لأن وقت الانتهاء (`expiresAt`) مخزَّن كطابع زمني مطلق وليس عداد تنازلي بالذاكرة.
- **سجلات للإدارة**: كل عملية تفعيل ناجحة تُسجَّل في `subscription_logs.json` (من فعّل، متى، أي باقة، أي كود)، وتُرسل أيضاً كـ Embed في `SUBSCRIPTION_LOG_CHANNEL_ID` إن تم ضبطه.
- **توليد أكواد جديدة**: `node scripts/generate-codes.js` يُكمل كل باقة إلى 100 كود غير مستخدم (أو أي رقم تمرره كمعامل)، بدون ما يلمس أي كود موجود مسبقاً، ويصدّر الأكواد الجديدة فقط إلى `new-codes-export.txt` لسهولة نسخها للمتجر.

## إصلاحات الموقع (الجلسة والأمان)
- **الجلسات تُحفظ الآن على القرص** (`storage/sessions.json`) بدل الذاكرة فقط، فإعادة تشغيل السيرفر (نشر جديد، توقف مؤقت من الاستضافة) ما تعود تسجّل خروج المستخدمين فجأة.
- **تخفيف التحقق من صلاحيات Discord** إلى مرة كل 3 دقائق لكل جلسة بدل كل تنقل بين الصفحات — هذا يمنع "تعليق الصفحة ثم رجوعها" الناتج عن بطء أو تقييد مؤقت من Discord API، مع بقاء التحديث الدوري خلال دقائق قليلة.
- **Rate Limiting** بسيط بدون اعتماديات خارجية على: تسجيل الدخول، رد OAuth، البحث عن كود سكربت، التحميل، والتشفير — يمنع إغراق النقاط الحساسة من نفس الـIP.
- **التحقق من صيغة IP/دومين السيرفر** قبل تمريره لمحرك التشفير، لمنع حقن قيم غريبة داخل ملفات Lua الناتجة.
- **سقف عدد عمليات التشفير لكل باقة يُطبَّق على تشفير الموقع أيضاً** (وليس فقط تشفير الديسكورد)، عبر قراءة نفس `permissions.json`، مع منع تنفيذ طلبي تشفير متزامنين لنفس المستخدم.
- **رؤوس أمان أساسية** (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`) على كل الردود.
- لا يوجد ضمان بخلو أي موقع من الثغرات 100%، لكن كل الصلاحيات (تسجيل الدخول، التشفير، لوحة الأدمن) يُعاد التحقق منها من الخادم دائماً، وليس فقط من الواجهة.
